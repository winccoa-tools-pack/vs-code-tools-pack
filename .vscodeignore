# Exclude git and GitHub metadata
.git/
.github/

# Node modules and lockfiles
node_modules/
package-lock.json
yarn.lock

# Documentation and local development files
docs/
# README.md is packaged so the extension includes the overview and images

# Tests and CI
tests/
*.spec.js

# Editor and OS files
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
Thumbs.db
*.DS_Store

# VS Code extension build artifacts
*.vsix
